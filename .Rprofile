.First <- function() {

    # automatically load these pkgs
    library(stats)
    library(ggplot2)

    # set default repo
    local({r <- getOption("repos")
        r["CRAN"] <- "http://cran.us.r-project.org"
        options(repos=r)})
}
